<section
  class="landing-hero landing-panel"
  aria-labelledby="landing-hero-heading"
>
  <div
    class="landing-hero__aurora landing-hero__aurora--primary"
    aria-hidden="true"
  >
  </div>
  <div
    class="landing-hero__aurora landing-hero__aurora--accent"
    aria-hidden="true"
  >
  </div>
  <div class="landing-hero__grid">
    <div class="landing-hero__copy">
      <p class="landing-kicker">Model. Generate. Ship.</p>
      <h1 id="landing-hero-heading">
        Browser test automation that feels like building product.
      </h1>
      <p class="landing-hero__description">
        AppraiseJS helps engineering teams model journeys visually, generate
        real Playwright + Cucumber artifacts, and keep quality pipelines
        deterministic as test coverage scales.
      </p>
      <div class="landing-hero__actions">
        <a class="landing-cta-button" href="/getting-started/quick_start/">
          Get Started
        </a>
        <a class="landing-secondary-button" href="/getting-started/overview/">
          Watch Demo
        </a>
      </div>
    </div>
    <div class="landing-product-visual landing-card" aria-hidden="true">
      <div class="landing-product-visual__header">
        <div class="landing-product-visual__dots">
          <span></span><span></span><span></span>
        </div>
        <span class="landing-product-visual__label">AppraiseJS</span>
      </div>
      <div class="landing-product-visual__body">
        <div class="landing-product-visual__row">
          <span>Test Run</span>
          <strong>Checkout Flow Validation</strong>
        </div>
        <div class="landing-product-visual__meter">
          <span style="--fill: 82%;"></span>
        </div>
        <ul>
          <li>
            <span class="state success"></span>
            <code>Validate cart contents</code>
            <small>10 test cases</small>
          </li>
          <li>
            <span class="state success"></span>
            <code>Validate checkout process</code>
            <small>13 test cases</small>
          </li>
          <li>
            <span class="state pending"></span>
            <code>Validate payment process</code>
            <small>12 test cases</small>
          </li>
        </ul>
      </div>
    </div>
  </div>
</section>

<style is:global>
  .landing-hero {
    isolation: isolate;
    margin-top: 0.2rem;
  }

  .landing-hero__grid {
    position: relative;
    display: grid;
    grid-template-columns: 1fr;
    align-items: center;
    gap: clamp(1.5rem, 5vw, 3rem);
  }

  .landing-hero__copy h1 {
    margin: 0.75rem 0 1rem;
    font-size: clamp(2.1rem, 6vw, 4.05rem);
    font-weight: 750;
    line-height: 1;
    letter-spacing: -0.035em;
    color: hsl(var(--foreground));
    max-width: 14ch;
  }

  .landing-hero__description {
    margin: 0;
    max-width: 54ch;
    color: hsl(var(--muted-foreground));
    font-size: clamp(1rem, 2vw, 1.12rem);
    line-height: 1.6;
  }

  .landing-hero__actions {
    margin-top: 1.4rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem;
  }

  .landing-product-visual {
    position: relative;
    overflow: hidden;
    padding: 1rem;
    border-color: hsl(var(--border) / 0.95);
    box-shadow: 0 30px 60px hsl(var(--background) / 0.55);
  }

  .landing-product-visual::before {
    content: "";
    position: absolute;
    inset: -40% -35% auto;
    height: 15rem;
    background: radial-gradient(
      closest-side,
      hsl(var(--primary) / 0.3),
      transparent
    );
    pointer-events: none;
  }

  .landing-product-visual__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid hsl(var(--border));
    padding-bottom: 0.8rem;
  }

  .landing-product-visual__dots {
    display: inline-flex;
    gap: 0.35rem;
  }

  .landing-product-visual__dots span {
    width: 0.62rem;
    height: 0.62rem;
    border-radius: 9999px;
    background: hsl(var(--muted-foreground) / 0.5);
  }

  .landing-product-visual__label {
    font-size: 0.78rem;
    color: hsl(var(--muted-foreground));
  }

  .landing-product-visual__body {
    position: relative;
    padding-top: 1rem;
  }

  .landing-product-visual__row {
    display: flex;
    justify-content: space-between;
    gap: 0.6rem;
    font-size: 0.83rem;
    color: hsl(var(--muted-foreground));
  }

  .landing-product-visual__row strong {
    color: hsl(var(--card-foreground));
    font-weight: 600;
  }

  .landing-product-visual__meter {
    margin: 0.95rem 0 1rem;
    height: 0.48rem;
    border-radius: 9999px;
    background: hsl(var(--secondary));
    overflow: hidden;
  }

  .landing-product-visual__meter span {
    display: block;
    height: 100%;
    width: var(--fill);
    background: linear-gradient(
      to right,
      hsl(var(--primary)),
      hsl(var(--accent))
    );
  }

  .landing-product-visual ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.52rem;
  }

  .landing-product-visual li {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 0.55rem;
    padding: 0.55rem 0.6rem;
    border-radius: calc(var(--radius) - 0.3rem);
    border: 1px solid hsl(var(--border) / 0.8);
    background: hsl(var(--secondary) / 0.45);
  }

  .landing-product-visual li code {
    font-size: 0.76rem;
    color: hsl(var(--foreground));
  }

  .landing-product-visual li small {
    color: hsl(var(--muted-foreground));
    font-size: 0.72rem;
  }

  .landing-product-visual .state {
    width: 0.52rem;
    height: 0.52rem;
    border-radius: 9999px;
    display: inline-block;
  }

  .landing-product-visual .state.success {
    background: hsl(var(--accent));
  }

  .landing-product-visual .state.pending {
    background: hsl(var(--primary));
  }

  .landing-hero__aurora {
    position: absolute;
    border-radius: 9999px;
    filter: blur(70px);
    pointer-events: none;
    z-index: -1;
  }

  .landing-hero__aurora--primary {
    width: 20rem;
    height: 20rem;
    top: -6rem;
    left: -5rem;
    background: hsl(var(--primary) / 0.25);
  }

  .landing-hero__aurora--accent {
    width: 16rem;
    height: 16rem;
    right: -4rem;
    bottom: -6rem;
    background: hsl(var(--accent) / 0.2);
  }

  @media (min-width: 64rem) {
    .landing-hero__grid {
      grid-template-columns: minmax(0, 1.12fr) minmax(22rem, 0.88fr);
    }
  }
</style>
