---
import { Check, Copy } from "@lucide/astro";
import AllInOneWorkspaceDemo from "./AllInOneWorkspaceDemo.astro";

const installCommand = "npx create-appraisejs@latest";
---

<section
  class="landing-hero landing-shell"
  aria-labelledby="landing-hero-heading"
>
  <div class="landing-hero__glow" aria-hidden="true"></div>
  
  <div class="landing-hero__content">
    <div class="landing-hero__copy">
      <p class="landing-kicker">Open source visual test automation</p>
      <h1 id="landing-hero-heading">
        Bridge QA and Engineering with visual tests you can actually version control.
      </h1>
      <p class="landing-hero__description">
        Visually author browser flows and generate reviewable Playwright/Cucumber
        artifacts your team can run locally, inspect in git, and carry into CI.
      </p>
      
      <div class="landing-hero__install" aria-label="Install command">
        <pre
          class="landing-hero__command"><code data-hero-install-command><span class="token-key">npx</span><span class="token-command"> create-appraisejs@latest</span></code></pre>
        <button
          type="button"
          class="landing-hero__copy-button"
          data-hero-copy-command
          aria-label="Copy install command"
        >
          <Copy
            class="landing-hero__copy-icon"
            size={16}
            stroke-width={2}
            aria-hidden="true"
          />
          <Check
            class="landing-hero__copy-icon landing-hero__copy-icon--success"
            size={16}
            stroke-width={2}
            aria-hidden="true"
          />
        </button>
      </div>
      
      <div class="landing-hero__workflow-cta">
        <p class="landing-hero__workflow-prompt">Curious how it works?</p>
        <a class="landing-secondary-button" href="#features-showcase">
          See the workflow
        </a>
      </div>
    </div>

    <div class="landing-hero__visual" aria-hidden="true">
      <div class="landing-hero__visual-glow"></div>
      <div class="landing-hero__visual-inner">
        <AllInOneWorkspaceDemo fill={true} />
      </div>
    </div>
  </div>
</section>

<script define:vars={{ installCommand }}>
  function initHeroCopy() {
    const button = document.querySelector("[data-hero-copy-command]");
    if (!button) return;

    let resetTimer;

    button.addEventListener("click", async () => {
      window.clearTimeout(resetTimer);

      try {
        await navigator.clipboard.writeText(installCommand);
      } catch {
        const code = document.querySelector("[data-hero-install-command]");
        const range = document.createRange();
        range.selectNodeContents(code);
        const selection = window.getSelection();
        selection?.removeAllRanges();
        selection?.addRange(range);
        document.execCommand("copy");
        selection?.removeAllRanges();
      }

      button.classList.add("is-copied");
      button.setAttribute("aria-label", "Copied");

      resetTimer = window.setTimeout(() => {
        button.classList.remove("is-copied");
        button.setAttribute("aria-label", "Copy install command");
      }, 2000);
    });
  }

  document.addEventListener("astro:page-load", initHeroCopy);
  initHeroCopy();
</script>
