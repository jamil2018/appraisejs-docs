---
title: Creating Tests
description: Create your first maintainable AppraiseJS scenario from module setup through saved case.
sidebar:
  order: 1
---

import DocHero from "../../../components/DocHero.astro";
import TaskChecklist from "../../../components/TaskChecklist.astro";
import OutcomeCard from "../../../components/OutcomeCard.astro";
import UIPath from "../../../components/UIPath.astro";
import MediaPlaceholder from "../../../components/MediaPlaceholder.astro";

<DocHero
  eyebrow="Hands-on tutorial"
  summary="This tutorial is the canonical first success journey for AppraiseJS. It shows how authored entities fit together while you build one real scenario."
  meta={[
    "Role: QA authors first, developers second",
    "Time: 20-30 minutes",
    "Outcome: one reusable login smoke test",
  ]}
  actions={[
    { label: "Open Quick Start", href: "/getting-started/quick_start/" },
    {
      label: "Open Running Tests",
      href: "/tutorials/running_tests/",
      variant: "secondary",
    },
  ]}
/>

## Before you begin

<TaskChecklist
  showTitle={false}
  items={[
    "AppraiseJS is running locally.",
    "You have a reachable login page or another small smoke-test target.",
    "You know which environment URL and credentials are safe to use locally.",
  ]}
/>

## What you'll build

<OutcomeCard title="A reusable smoke scenario" tone="success">
  You will create one module, one environment, one locator group, four locators,
  one suite, and one test case that can be rerun from the Test Runs screen.
</OutcomeCard>

## Step-by-step procedure

<UIPath
  steps={["Modules", "Environments", "Locators", "Test Suites", "Test Cases"]}
/>

1. Create module `Authentication` so the scenario has a clear home in the product model.
2. Create environment `Local` with the application base URL you want to test.
3. Open `Locators -> Create` and launch the Chromium picker from that saved environment.
4. Capture locators for `emailInput`, `passwordInput`, `submitButton`, and `welcomeBanner`.
5. Accept the suggested locator group for the login route, or create `Login Page` if nothing relevant exists yet.
6. Create suite `Auth Smoke` under the `Authentication` module.
7. Create case `Valid login shows welcome banner` and attach it to the `Auth Smoke` suite.
8. Add the first step to navigate to the selected environment base URL so the case is portable across environments.
9. Add input steps for email and password using the captured locators.
10. Add a click step for the submit button.
11. Add an assertion step that checks the `welcomeBanner` visibility is `true`.
12. Save the case and re-open it once to confirm every parameter, locator reference, and suite relationship still looks correct.

The first visual checkpoint in this tutorial is the saved entity state. Readers need to see which records should already exist before the case editor becomes the main focus.

<MediaPlaceholder
  type="screenshot"
  title="Saved entity setup with environment, module, suite, and case already created"
  why="Which records need to exist before the test case editor becomes productive?"
  priority="must-have"
  context="Creating Tests -> Step-by-step procedure after entity creation"
  capture="Capture the saved state of the environment, module, suite, and case created in this tutorial so the dependency chain is obvious."
  successState="Each required entity is visibly saved, named, and connected closely enough that a reader can confirm they are ready to author steps."
/>

The next ambiguity is the locator picker itself. Prose can explain the steps, but the overlay, pick confirmation, and return to the form are easier to trust when shown as motion.

<MediaPlaceholder
  type="gif"
  title="Locator picker flow from launch to selector confirmation"
  why="What exactly happens between launching the picker and returning with a confirmed locator?"
  priority="must-have"
  context="Creating Tests -> Locator capture flow"
  capture="Record launching the Chromium picker from the saved environment, selecting an element, reviewing the selector in the overlay, and confirming the pick."
  successState="The overlay, picked element state, and confirmation back into the locator workflow are all visible."
/>

Once the locators exist, users need to recognize what a valid runnable case looks like while it is being assembled. The editor clip should make ordered steps, parameters, and the final save state concrete.

<MediaPlaceholder
  type="gif"
  title="Case editor walkthrough with ordered steps, parameter entry, and save"
  why="What visual state tells me this case is complete enough to save and run?"
  priority="must-have"
  context="Creating Tests -> Test case editing"
  capture="Record adding the ordered login steps, filling required parameters, saving the case, and reopening it once to show the saved step flow."
  successState="The case shows a complete ordered step sequence with filled parameters and a saved state that reads as runnable."
/>

If all three checkpoints match, the reader should be able to move into [Running Tests](/tutorials/running_tests/) without wondering whether the failure is in authoring or execution.

## Checkpoint

- The case opens with a complete ordered flow and no missing required parameters.
- Every referenced locator exists and belongs to a route-aware locator group.
- The suite, module, and environment names are clear enough that another team member could reuse them later.

## Common mistakes

- Creating locators before deciding which environment or route they should belong to.
- Saving a case with literal values that should have been environment-driven or reusable.
- Mixing multiple business outcomes into one case instead of one clear scenario.
- Naming assets too generically, which makes future reuse harder.

## Next step

Move to [Running Tests](/tutorials/running_tests/) to execute the scenario either by tags or by explicit case selection.
