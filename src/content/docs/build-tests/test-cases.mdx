---
title: Test Cases
description: Compose one business outcome into an executable step flow.
sidebar:
  order: 10
---

import MediaPlaceholder from "../../../components/MediaPlaceholder.astro";

A test case is a single scenario composed of ordered template steps.

## Workflow context

Test cases are where reusable assets become a specific business scenario. This is the point where modules, suites, locators, template steps, and tags come together.

## Do this

1. Go to `Test Cases -> Create`.
2. Add title (min 3 chars) and description.
3. Select one or more test suites.
4. Optionally add tags.
5. Build the flow in the diagram editor.
6. Fill required parameters for each step.
7. Save.

This guide is still a likely entry point for users who jump in from locator or suite documentation, so one editor-state placeholder is warranted. It should show what "complete enough to save" looks like before the reader assembles a broken case.

<MediaPlaceholder
  type="screenshot"
  title="Saved test case editor state with ordered steps and filled parameters"
  why="What should a runnable test case look like before I click save?"
  priority="should-have"
  context="Test Cases guide -> Editor completion state"
  capture="Capture the test case editor with a complete ordered step flow, filled required parameters, and a saved state that still shows suite and tag context."
  successState="The case reads as one finished business scenario and gives the reader a concrete success target before execution."
/>

Once this state is visible, the reader should be able to confirm success by reopening the saved case and checking that every locator reference and required parameter survived.

## Minimal example

- Title: `Valid login shows welcome banner`
- Suite: `Auth Smoke`
- Steps: navigate, fill email, fill password, click submit, assert banner visible

## What good looks like

- Keep one business outcome per test case.
- Use clear step labels and stable locator references.
- Use template test cases for repeated patterns.

## Next step

- [Test Runs](/run-and-analyze/test-runs/)
- [Template Test Cases](/build-tests/template-test-cases/)
