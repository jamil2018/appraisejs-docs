---
title: Quick Start
description: Build and run your first AppraiseJS scenario after completing the recommended production-first local setup flow.
sidebar:
  order: 3
---

import DocHero from "../../../components/DocHero.astro";
import TaskChecklist from "../../../components/TaskChecklist.astro";
import OutcomeCard from "../../../components/OutcomeCard.astro";
import UIPath from "../../../components/UIPath.astro";
import MediaPlaceholder from "../../../components/MediaPlaceholder.astro";

<DocHero
  eyebrow="First run"
  summary="This page is the fastest guided route from a running local app and a clean scaffold to a passing scenario with a report you can inspect."
  meta={[
    "Role: first-time QA authors",
    "Time: 15 minutes",
    "Outcome: one runnable login flow",
  ]}
  actions={[
    { label: "Install first", href: "/getting-started/installation/" },
    {
      label: "Open Creating Tests",
      href: "/tutorials/creating_tests/",
      variant: "secondary",
    },
  ]}
/>

## Before you begin

<TaskChecklist
  showTitle={false}
  items={[
    "AppraiseJS is installed and `npm run start` opens the local app.",
    "You have a reachable application URL for a simple login or smoke scenario.",
    "You can create local test data or use non-sensitive credentials.",
  ]}
/>

## What you'll finish with

<OutcomeCard title="First successful run" tone="success">
  You will leave this page with an environment, a module, locators, a test
  suite, a test case, one test run, and one report you can inspect.
</OutcomeCard>

New scaffolds start with a seeded local database and a clean automation
workspace. That means the steps below create your first real environment,
locator group, suite, and case instead of modifying bundled starter features or
reports.

## Step-by-step procedure

<UIPath
  steps={[
    "Environments",
    "Modules",
    "Locators",
    "Test Suites",
    "Test Cases",
    "Test Runs",
    "Reports",
  ]}
/>

1. Start AppraiseJS with `npm run start` and confirm the local app opens in your browser. Use `npm run dev` only if you intentionally need the dev-server workflow while iterating locally.
2. Create environment `Local` with the base URL of the application you want to test.
3. Create module `Authentication` or another feature-oriented module name.
4. Open `Locators -> Create` and launch the Chromium picker from the saved environment.
5. Capture at least these locators: `emailInput`, `passwordInput`, `submitButton`, and one success-state locator such as `welcomeBanner`.
6. Accept the suggested locator group if the route is already recognized, or create a new group such as `Login Page`.
7. Create test suite `Auth Smoke`.
8. Create test case `Valid login shows welcome banner`.
9. Add these steps in order: navigate to selected environment base URL, fill email input, fill password input, click submit button, assert success-state visibility.
10. Save the test case and open `Test Runs -> Create`.
11. Create a run against `Local`, submit it, and watch status changes on the run details page.
12. Open the linked report and confirm the scenario outcome matches what happened in the browser.

Before the first run, users should be able to compare their authored setup against a known-good finished state. This prevents guessing about whether they created enough entities or saved them in the right places.

<MediaPlaceholder
  type="screenshot"
  title="Completed Quick Start state with the first saved environment, module, locator group, suite, and case"
  why="What should 'finished setup' look like before I create a run?"
  priority="must-have"
  context="Quick Start -> Step-by-step procedure after authoring"
  capture="Capture a clean project state where the environment, module, locator group, suite, and case created in this tutorial are all visible and saved."
  successState="Named entities are visible in the UI, clearly saved, and recognizable as the minimum complete setup for the first run."
/>

After that state is confirmed, the next confusion point is the handoff from authoring to execution. The motion clip should make the screen transitions explicit instead of assuming users will infer them.

<MediaPlaceholder
  type="gif"
  title="Quick Start handoff from saved case to submitted run to opened report"
  why="How do I move from a saved test case into a completed run and the first report without getting lost between screens?"
  priority="must-have"
  context="Quick Start -> Step-by-step procedure after run creation"
  capture="Record saving the completed case, opening run creation, submitting the run, waiting through the first useful status change, and opening the resulting report."
  successState="Each transition is visible enough that a new user can name the next screen to open after saving the case."
/>

If the saved-state screenshot and the handoff clip match what the reader sees locally, they should be able to continue into [Creating Tests](/tutorials/creating_tests/) without guessing whether the scaffold is still in a healthy state.

## Checkpoint

- You can open one environment, one module, one locator group, one suite, and one case from the UI.
- Your test run either completed successfully or failed with a report and logs you can inspect.
- You know whether you need to continue to [Creating Tests](/tutorials/creating_tests/) or jump to [Troubleshooting](/operations/troubleshooting/).

## Common mistakes

- Expecting the scaffold to include starter environments, locators, test cases, or reports.
- Launching the picker from the wrong base URL and saving locators for the wrong page.
- Creating a run before the test case has all required step parameters.
- Forgetting that AppraiseJS uses the selected environment during navigation and wait steps.
- Skipping `Settings -> Sync` after editing generated files outside the UI.

## Next step

Continue to [Creating Tests](/tutorials/creating_tests/) to turn this short checklist into a repeatable authoring workflow.
