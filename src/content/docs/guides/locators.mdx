---
title: Locators
description: Capture stable selectors with the picker or create them manually with enough context to survive reruns.
sidebar:
  order: 5
---

import DocHero from "../../../components/DocHero.astro";
import UIPath from "../../../components/UIPath.astro";
import MediaPlaceholder from "../../../components/MediaPlaceholder.astro";

<DocHero
  eyebrow="Build tests"
  summary="Use locators to give your steps stable element references. In AppraiseJS, the best path is usually route-aware capture through the Chromium picker, not typing selectors blind."
  meta={[
    "Job to be done: capture a selector that stays useful later",
    "Best for: page-level UI interactions",
    "Related workflow: locator groups and test steps",
  ]}
  actions={[
    { label: "Open Locator Groups", href: "/guides/locator_groups/" },
    {
      label: "Open Creating Tests",
      href: "/tutorials/creating_tests/",
      variant: "secondary",
    },
  ]}
/>

## Workflow context

```nomnoml
#direction: right
#stroke: #64748b
#fill: #f8fafc
[Locator Group] -> [Locator]
[Locator] -> [Template Step Parameter]
[Template Step Parameter] -> [Test Case]
```

The locator creator matters because it sits between page discovery and durable test authoring. A good locator makes the case editor faster and the rerun more reliable.

## Do this

<UIPath
  steps={[
    "Locators",
    "Create",
    "Launch Chromium Picker",
    "Finalize Locator",
    "Save",
  ]}
/>

1. Open `Locators -> Create`.
2. Choose whether to launch from a saved environment or a direct URL.
3. Start the Chromium picker and navigate to the target page state if needed.
4. Click `Start picking`, select the element, review the proposed selector, then confirm it.
5. Review the autofilled locator name, selector value, route, and locator group suggestion.
6. Save the locator to an existing group or create a new route-aware group before saving.

The create screen itself should make it obvious that picker launch and final review belong to one workflow. Use the screenshot below to show both the entry point and the fields that matter after capture.

<MediaPlaceholder
  type="screenshot"
  title="Create Locator screen with picker launch and final review fields"
  why="Where do I start the picker, and what parts of the locator form should still be visible after capture?"
  priority="must-have"
  context="Locators -> Create workflow"
  capture="Capture the Create Locator page showing the picker launch entry point and the locator-definition fields that are reviewed before save."
  successState="The viewer can see that picker launch and locator review are part of one connected workflow."
/>

The picker interaction itself is still the most motion-heavy part of locator authoring. Keep the clip short and focused on the overlay, selection, and return path.

<MediaPlaceholder
  type="gif"
  title="Picker interaction from launched browser to confirmed selector"
  why="What do I click in the picker overlay, and how does a successful pick behave?"
  priority="must-have"
  context="Locators -> Picker interaction"
  capture="Record launching the picker, selecting an element, reviewing the selector in the overlay, and confirming the selection."
  successState="The overlay state and the confirmation moment are both visible enough that a user could repeat the interaction."
/>

After the pick returns, readers still need to know whether the autofilled state looks healthy or brittle. The final screenshot should make that review step explicit before save.

<MediaPlaceholder
  type="screenshot"
  title="Prefilled post-capture state with suggested name, selector, route, and group"
  why="What should a good post-capture locator state look like before I save it?"
  priority="must-have"
  context="Locators -> Post-capture review"
  capture="Capture the form immediately after a successful pick so the suggested name, selector, route, and locator group are all visible."
  successState="The prefilled values look reviewable and route-aware, making it clear what should be checked before save."
/>

If the prefilled state does not resemble this once captured, the user should stop and review selector quality before continuing to [Test Cases](/guides/test_cases/).

## Minimal example

- Group: `Login Page`
- Route: `/login`
- Locators: `emailInput`, `passwordInput`, `submitButton`, `welcomeBanner`

## What good looks like

- The locator name is readable and specific.
- The selector is stable enough to survive layout changes.
- The locator group route matches the page where the step actually uses the selector.

## Next step

Continue to [Test Cases](/guides/test_cases/) to see how locator references become executable steps.
