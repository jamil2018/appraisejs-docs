---
title: Test Runs
description: Create a focused execution with clear scope, environment, and runtime settings.
sidebar:
  order: 11
---

import MediaPlaceholder from "../../../components/MediaPlaceholder.astro";

Test runs execute cases by tag selection or explicit case selection.

## Workflow context

Test runs are the handoff from authored assets into execution. This is where you decide exactly what should run and under which conditions.

## Do this

1. Go to `Test Runs -> Create`.
2. Choose selection mode (`By Tags` or `By Test Cases`).
3. Provide run `Name`.
4. Select `Environment`.
5. Set `Test Workers Count` (minimum 1).
6. Select `Browser Engine` (`CHROMIUM`, `FIREFOX`, `WEBKIT`).
7. Submit.

If this guide is the reader's entry point, they still need one screen reference before they submit the run. Keep the capture brief here so the tutorial and the guide point to the same UI expectation.

<MediaPlaceholder
  type="screenshot"
  title="Test Runs creation screen with selection mode, environment, and runtime settings"
  why="What should the run setup screen look like before I submit?"
  priority="should-have"
  context="Test Runs guide -> Create flow"
  capture="Capture the run creation screen with the selection mode control, environment field, worker count, browser engine, and submit action visible."
  successState="A reader can recognize the main execution inputs and confirm they are on the correct screen before submitting."
/>

Once that screen is captured, readers should be able to confirm success by opening the details screen and matching the final scope to what they selected.

## Minimal example

- Name: `auth-smoke-local-chromium`
- Selection mode: `By Tags`
- Tag: `@smoke`
- Environment: `Local`

## What good looks like

Use the details screen to inspect:

- Current run status and result.
- Linked test cases and selection scope.
- Run logs and trace artifacts.
- Linked report access after completion.

## Next step

- [Running Tests](/getting-started/running-tests/)
- [Reports](/run-and-analyze/reports/)
