---
title: Reports
description: Read execution outcomes quickly enough to decide what to fix next.
sidebar:
  order: 12
---

import MediaPlaceholder from "../../../components/MediaPlaceholder.astro";

Reports are generated from Cucumber JSON output after run completion.

## Workflow context

Reports are the shortest route from a finished run to a useful debugging decision. Start here before you open raw logs or traces.

## Do this

- `Reports` list page: run-level summary table.
- Report detail page: pass/fail totals, environment details, scenario-level breakdown.
- Metric report: `/reports/test-cases`
- Metric report: `/reports/test-suites`

If a user lands on this guide without the full tutorial, they still need to know what the primary report screen should contain. Keep one summary screenshot request here rather than forcing them to imagine the layout from labels alone.

<MediaPlaceholder
  type="screenshot"
  title="Reports summary screen with totals and failed scenario rows"
  why="Which report view should I read first before I open logs or traces?"
  priority="should-have"
  context="Reports guide -> Primary summary view"
  capture="Capture a report summary screen with run-level totals, execution context, and at least one failed scenario row visible."
  successState="A reader can identify the report summary as the first stop for debugging and can see where scenario drill-down begins."
/>

Once that summary is recognizable, the rest of the guide should read as a decision flow: start with the report, then drill into detail, then escalate to deeper artifacts only when needed.

## Minimal example

- Open the newest failed report.
- Read summary totals and scenario rows.
- Open logs or traces only for the failed scenario that still looks ambiguous.

## What good looks like

1. Start from failed report rows.
2. Drill into scenario/step details.
3. Open run logs and Playwright traces for root-cause analysis.

## Next step

- [Viewing Reports](/getting-started/viewing-reports/)
- [Dashboard](/run-and-analyze/dashboard/)
